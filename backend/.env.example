PORT=4000
CLIENT_ORIGIN=http://localhost:5173,http://localhost:5174
DATABASE_URL=postgres://rowan:rowanpass@localhost:5433/rowan_portal
JWT_SECRET=change-me
OTP_TTL_MINUTES=5
EXPOSE_OTP=true

# Read-only connection to the live CRM (shared RDS, public schema). Uses the
# locked-down portal_ro role (SELECT-only). Leave unset to disable CRM reads.
# CRM_DATABASE_URL=postgres://portal_ro:PASSWORD@HOST:5432/client_credentials?sslmode=no-verify
CRM_POOL_MAX=8
