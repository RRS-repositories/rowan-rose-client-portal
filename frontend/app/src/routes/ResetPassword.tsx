import { AuthPlaceholder } from "@/components/layout/AuthPlaceholder";

export default function ResetPassword() {
  return <AuthPlaceholder icon="password" title="Reset password" phase="Coming in Phase 1.4" />;
}
